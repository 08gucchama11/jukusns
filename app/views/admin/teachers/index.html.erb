<h1>講師一覧</h1>

<%= link_to "講師登録フォーム", new_admin_teacher_path %>

<% @teachers.each do |teacher| %>
  <p>
    <%= teacher.name %>
    <%= teacher.code %>
    <%= link_to "詳細", admin_teacher_path(teacher) %>
  </p>
<% end %>