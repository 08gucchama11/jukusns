<h1>生徒一覧</h1>

<%= link_to "生徒登録フォーム", new_admin_student_path %>

<% @students.each do |student| %>
  <p>
    <%= student.name %>
    <%= student.code %>
    <%= link_to "詳細", admin_student_path(student) %>
  </p>
<% end %>